# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.


common: &common
  #adapter: postgresql
  username: ubuntu
  password: inpontopostgres # from psql setup, see Postgresql
  timeout: 5000

#development:
#  <<: *common
#  database: inponto_development

#test:
#  <<: *common
#  database: inponto_test

production:
  <<: *common
  database: inponto
